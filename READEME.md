TypeScriptとReactとNext.jsとNode,jsとTailwindの練習環境


## 起動方法
1. リポジトリをクローン
2. ルートディレクトリで以下を実行
   ```bash
   docker-compose up -d --build


   #### 2. 各サービスのURLとポート番号
今の「2つに分かれている」状態をメモしておきましょう。
```markdown
## サービス構成
- **Frontend**: http://localhost:3000 (Next.js)
- **Backend**: http://localhost:8000 (Express/tsx)
- **Database**: localhost:5432 (PostgreSQL)


3. 技術スタック（使用ツール）
今回苦労して導入した tsx や cors も書いておくと、自分のスキルの棚卸しになります。

Markdown
## 技術スタック
- **Frontend**: Next.js (App Router), Tailwind CSS
- **Backend**: Node.js, Express, TypeScript, tsx (watcher)
- **Infra**: Docker, Docker Compose


今の構成図（フロント ↔ バック ↔ DB）



今はまだ DB 接続が「準備中」なので、README に 「## 今後の予定: PostgreSQL との接続、
TypeScriptの初心者練習（新規作成、更新、作成、アップデート）の実装。
README の準備ができたら、いよいよ git add . して GitHub へ初プッシュですね！



【条件】
AI使って環境構築はしてもいい
プログラムからはＡＩ使わずにｗｅｂのみで練習する
（削除、更新、新規作成、アップデート）　→クラッドの練習（アルゴリズムやフローチャートを鍛えたいから）


目的は、開発する為の基礎知識を鍛える上でアルゴリズム強化、基礎を意識、正しいトライアンドエラーをする、フローチャートを意識する。



🚀 最終確認：ちゃんと動いているかチェック
ブラウザを開いて、以下の2つのURLにアクセスしてみてください。

1. フロントエンド（Next.js）
URL: http://localhost:3000

http://localhost:3000



成功時の画面: Next.jsのロゴが表示された黒っぽい（または白っぽい）デフォルト画面。

2. バックエンド（API）
URL: http://localhost:8000

http://localhost:8000



成功時の画面: {"message": "Hello from Backend! 勤怠システム準備中..."} という文字が表示。



★「フロントエンド」と「バックエンド」がそれぞれ別のコンテナ（独立したサーバー）として動いている状態です。

1. 今、どういう状態なの？
図にすると、あなたのパソコンの中で以下のようなことが起きています。

コンテナ1 (frontend): Next.jsが動いています。ユーザーが見る「画面」を作る担当です。

コンテナ2 (backend): Express (Node.js) が動いています。勤怠の計算やデータの保存などの「計算・ロジック」担当です。

コンテナ3 (db): データを保存する倉庫です。



CORSとは？  （コルス）
CORS (Cross-Origin Resource Sharing) は、ブラウザが持っている「セキュリティの番人」のような仕組みです。

例えば、http://localhost:3000（フロント）が、勝手に http://localhost:8000（バック）のデータを読み書きしようとすると、ブラウザが**「ちょっと待て！許可されてないサイトからのアクセスだぞ！」**とブロックしてしまいます。

これを解決するには、バックエンド側で「ポート3000からのアクセスならOKだよ！」という**「許可証」**を発行してあげる必要があります。

コルスのパッケージのインストールを行います。



2. CORSの役割を図解
なぜ今 cors の設定をしているのか、そのイメージはこんな感じです。

ブラウザはセキュリティのため、**「3000番のフロントから来た命令で、勝手に8000番のバックエンドのデータを盗ませない」**という壁を作っています。cors を設定することで、バックエンド側から「3000番さんなら信頼できるから通していいよ」という許可証を出すイメージです。




🛠️ これで「開発の土台」が完成しました！
現在、あなたの環境は以下のようになっています：

フロントエンド (Next.js): ユーザーが見る画面担当

バックエンド (Express): 処理や計算担当

データベース (PostgreSQL): データを保存する倉庫（まだ繋いでません）

CORS設定: フロントとバックの安全な通信路



★フロントとバックでコルスのパッケージが揃ってなかったので揃えたら表示成功されました。




【動作確認方法】

項目　　　以前のスタイル (Monolith)　　　　今のスタイル (Decoupled)

URL　　　localhost:3000 だけ見ればOK　　　localhost:3000 だけ見ればOK (重要！)

役割　　　画面もデータも1つのURLが返す　　　3000番は「見た目」を返し、裏で8000番に「データ」を取りに行く

確認方法　　　ブラウザの表示のみ　　　　　　ブラウザの表示 + デベロッパーツールの「ネットワーク」




